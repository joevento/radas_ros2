services:
  ros2_dev:
    build: .
    container_name: ros2_jazzy_container
    network_mode: host
    volumes:
      - ./src:/ros2_ws/src
      - /tmp/.X11-unix:/tmp/.X11-unix
    environment:
      - DISPLAY=$DISPLAY
    stdin_open: true
    tty: true
    privileged: true
